import React, { memo } from 'react'
import {dicoverMenu} from '@/services/local-data'
import {
  TopMenu,
  DiscoverWrapper
} from './style'
const Main = memo((props) => {
  return (
    <div>Main</div>
  )
})

export default Main